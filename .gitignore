# 개인 기록과 생성물 — 공개 저장소에 올라가지 않게 합니다
profile.md
data/
output/
archive/
logs/
# Cloudflare 올리기: 비밀번호(토큰)가 든 설정과 올릴 폴더 — 절대 git 에 올리지 않음
cloudflare.json
site/
__pycache__/
*.pyc

# 자동으로 받은 그림(각자 PC에서 생성)
assets/*_auto.jpg
# 내가 직접 넣은 그림(저작권이 있는 그림일 수 있어 공개 저장소에 올리지 않음)
assets/court.*
assets/portrait.*
assets/portraits/*
!assets/portraits/*_default.jpg
